---
deployment:
  quotation:
    - export DEPLOYPATH=/home/biderne1/public_html/quotation.bidernet.co.il
    - /usr/bin/rsync -a --exclude='.git' --exclude='.gitignore' --exclude='.cpanel.yml' --exclude='config.php' --exclude='uploads/' --exclude='.htaccess' ./ "$DEPLOYPATH/"
    - /bin/chmod 755 "$DEPLOYPATH"
